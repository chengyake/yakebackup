# LUA位运算  
bit.bnot(a) - 返回一个a的补充\
bit.band(w,m) - 返回w的位与\
bit.bor(w,m) - 返回w的位或\
bit.bxor(w,m) - 返回w的位异或\
bit.lshift(a,b) - 返回a向左偏移到b位\
bit.rshift(a,b) - 返回a逻辑右偏移到b位\
bit.arshift(a,b) - 返回a算术偏移到b位\
bit.mod(a,b) - 返回a除以b的整数余数

